# The GitHub Actions runner is Linux: keep LF in the repository for everything it
# executes, regardless of what Windows does in the working copy.
* text=auto eol=lf
*.ps1 text eol=crlf
*.png binary
*.jpg binary
